# WeatherIsland PRD

## 产品目标

WeatherIsland 是一个常驻在 macOS 屏幕顶部的天气信息窗口，视觉上参考 Dynamic Island / notch 融合体验。
产品需要在有 notch 和无 notch 的 Mac 设备上都保持自然、稳定、可读的交互表现。

## 核心交互

### 1. 常驻顶部显示

- 应用启动后，在屏幕顶部中央附近显示一个收起态天气胶囊。
- 收起态默认常驻显示，不需要激活主窗口。
- 鼠标移入收起态后，延时展开为更大的天气信息面板。
- 鼠标移出或点击外部区域后，展开态自动收起。

### 2. 收起态与展开态

- 收起态用于快速展示天气状态和温度。
- 展开态用于展示更多天气详情、位置、降雨提示以及字体切换菜单。
- 收起和展开之间使用平滑动画衔接。
- 收起动画的视觉中心应向中间聚拢，而不是向左上角裁切。

## Notch 兼容策略

### 3. 顶部 notch 判断

- `IslandPanelController.swift` 会先判断当前屏幕有没有顶部 notch。
- 只要检测不到 notch，就不会再走“中间避让区 + 左右辅助区域”的判断，而是直接回到普通居中的收起方案。
- 检测 notch 时，优先参考系统提供的顶部安全区域和辅助区域信息。

### 4. 有 notch 机型的布局规则

- 有 notch 时，收起态优先采用“中间避让 notch、左右显示内容”的布局。
- notch 避让区当前按约 `210 x 24` 的视觉空间设计。
- 如果顶部左右实际可用区域不足，说明菜单栏项目可能过多或存在遮挡风险，则自动切换到右侧小胶囊模式。
- 右侧小胶囊模式只显示简化后的天气信息。

### 5. 无 notch 机型的布局规则

- `WeatherIslandView.swift` 会跟随 notch 判断结果联动。
- 无 notch 时，中间 notch 避让空隙宽度变为 `0`，不会无意义地留出中间空洞。
- 无 notch 时默认使用普通居中的收起胶囊方案。

## 动态尺寸与视觉规则

### 6. 菜单栏高度自适应

- 收起态高度不是固定写死，而是基于系统菜单栏真实高度动态计算。
- 收起态 UI 需要确保完全处于菜单栏可接受的视觉范围内，并额外预留约 `8px` 缓冲。
- 不同设备型号、分辨率和显示缩放下，收起态都应自动适配。

### 7. 收起态文字与图标规则

- 收起态文字大小应与系统菜单栏选项字体一致。
- 当前实现使用系统菜单栏标准字体。
- 收起态图标、内边距、胶囊内容高度应随菜单栏高度一起缩放，以保证整体平衡。

### 8. 胶囊曲率规则

- 收起态窗口外轮廓必须表现为完整胶囊。
- 外层圆角应根据当前收起总高度动态计算，而不是固定值。
- 内层左右内容胶囊和右侧小胶囊也应根据自身高度保持一致曲率。
- 上下左右圆角应保持连续、统一、自然。

## 字体切换交互

### 9. 字体菜单行为

- 展开态顶部提供字体切换菜单。
- 字体菜单打开、菜单选择中、字体切换完成后的短时间内，窗口不应自动收起。
- 菜单 tracking、临时失焦、点击字体选项都不应打断当前展开态。

## 动画与状态切换

### 10. 动画原则

- 展开动画应有轻微放大和向下展开的感觉。
- 收起动画应向中心聚合，避免内容看起来朝左上角塌缩。
- 内容层动画和面板尺寸动画要保持一致，不出现明显错位。

## 主要运行时判断逻辑

### 11. 当前规则总结

- `IslandPanelController.swift` 负责窗口尺寸、位置、notch 判断、菜单栏高度适配和收起模式选择。
- `WeatherIslandView.swift` 负责根据控制器输出的状态切换收起布局、展开布局、notch 避让间距以及字体和动画表现。
- 收起布局在运行时有三种结果：
  - 有 notch 且空间充足：中间避让 notch 的双侧胶囊布局。
  - 有 notch 但顶部区域拥挤：右侧小胶囊布局。
  - 无 notch：普通居中的完整胶囊布局。

## 后续可扩展方向

- 把 notch 避让区尺寸做成可配置参数。
- 进一步优化右侧小胶囊的视觉细节和动画表现。
- 增加更多屏幕场景的布局策略，例如多显示器、超宽屏和外接显示器。
